# Training outputs
crystal-lora-output/
crystal-qwen-gguf/
crystal-qwen-mlx-q*/
runpod-pipeline-final/
runpod-pipeline-merged*/
runpod-adapter/
runpod-merged/

# Cloned upstream repos for CPT corpus mining (regenerated by fetch_training_sources.sh + build_cpt_corpus_v2.py)
crystal_corpus_raw/

# Downloaded adapter, merged model, and converted GGUF
together-adapter/
together-merged/
crystal-lora-adapter.gguf
*.gguf

# llama.cpp (cloned for converter)
llama.cpp/

# RunPod / Together AI state (contains pod IDs, file IDs, job IDs — keep local)
.runpod_state.json
.together_state.json

# Editor/IDE state
.crystal/
.claude/

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/

# MLX local-training artefacts
mlx_data/
mlx_data_v*/
mlx_adapters/
mlx_adapters_v*/
mlx_train.log
model_download*.log

# Logs and pidfiles
*.log
*.pid

# Comparison / verification outputs
comparison.json
comparison.log

# Hard-data scratch directory (regenerated by generate_hard_crystal_data.py)
hard_data/

# Large generated training data (regenerated by convert_training_data.py
# and the build_*.py scripts — track the generator, not the output)
training_data.jsonl
training_data_alpaca.jsonl
training_data_alpaca.json
training_data_together.jsonl
cpt_corpus.jsonl
cpt_corpus_v*.jsonl
cpt_docs.jsonl
dpo_pairs.jsonl
dpo_pairs_v*.jsonl
sft_v*.jsonl
sft_v*.partial.jsonl
sft_v*.before_llm.jsonl
